{
  "name": "quiz-game",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run backend:start\" \"npm run frontend:game-board\" \"npm run frontend:player\"",
    "backend:start": "cd backend && npm run build && npm start",
    "frontend:game-board": "cd frontend && npm run start:game-board",
    "frontend:player": "cd frontend && npm run start:player"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.22",
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.10",
    "postcss": "^8.5.6",
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "typescript": "^5.9.3"
  }
}
